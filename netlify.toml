[build]
command = "pnpm build"
publish = "dist"

[build.environment]
NODE_VERSION = "24"

# SPA fallback so client-side routes resolve to index.html
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
